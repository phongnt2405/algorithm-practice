#include <iostream>
#include <string>
#include <vector>

int main() {
  int h, w, i, j;
  std::cin >> h >> w >> i >> j;

  for (int i = 0; i < h; i++) {
    std::string s;
    std::cin >> s;

    for (int )
  }
}
