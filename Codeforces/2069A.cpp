#include <iostream>
#include <vector>

bool solve(std::vector<int>& arr, int n) {

}

int main() {
  std::ios::sync_with_stdio(0);
  std::cin.tie(0);

  int t;
  std::cin >> t;

  while (t--) {
    int n;
    std::cin >> n;

    std::vector<int> arr(n - 2);
    for (int i = 0; i < n - 2; i++) {
      std::cin >> arr[i];
    }

  }
}
